version: '3'
services:
  api:
    env_file:
      - .env
    build: .
    tty: true
    volumes:
      - .:/go/src/github.com/Cherry0202/RamenWikiStoreFunction
    # 環境変数確認
    command:
      - 'env'
    ports:
      - "8080:8080"